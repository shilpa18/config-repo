spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: my_super_secure_secret_key_12345678901234567890
  datasource:
    url: jdbc:mysql://localhost:3306/ProfitProjectorDb?createDatabaseIfNotExist=true
    username: root
    password: Pass@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
         

server:
  port: 8081
